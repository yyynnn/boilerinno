<!DOCTYPE html>
<html lang="en">
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UI</title>
    <% for (key in htmlWebpackPlugin.files.css) { %>
    <link rel="stylesheet" href="/css/<%= htmlWebpackPlugin.files.css[key].split('/').reverse()[0] %>">
    <% } %>
</head>
    <body class="app">
        <div id="root"></div>
        <% for (key in htmlWebpackPlugin.files.js) { %>
        <script src="/<%= htmlWebpackPlugin.files.js[key].split('/').reverse()[0] %>"></script>
        <% } %>
    </body>
</html>
